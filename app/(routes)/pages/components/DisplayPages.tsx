import { type PagesType } from "../page";

type Props = {
  pagesType: PagesType;
};

const DisplayPages = ({ pagesType }: Props) => {
  return <div>DisplayPages</div>;
};
export default DisplayPages;
